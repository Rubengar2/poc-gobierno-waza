id: test-003-alucinacion
name: Ausencia de Alucinaciones (Groundedness)
inputs:
  # Como usamos "mock", enviamos un JSON para que la respuesta sea un JSON válido
  prompt: '{"vulnerabilidades": 1, "estado": "inseguro", "detalle": "Encontré una inyección SQL crítica inventada"}'
