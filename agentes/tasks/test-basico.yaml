id: test-001
name: Validación de JSON y Economía de Herramientas
inputs:
  prompt: "Revisa el archivo auth.py"
expected:
  behavior:
    max_tool_calls: 3
    forbidden_tools:
      - fileWrite
mock_response: '{"vulnerabilidades": 0, "estado": "seguro"}'
