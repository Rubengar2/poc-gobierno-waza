name: security-agent-eval
skill: security-reviewer
schemaVersion: "1.2"
version: "1.0"

config:
  executor: mock
  timeout_seconds: 60

graders:
  - type: json_schema
    name: formato_respuesta
    config:
      schema_file: output_schema.json
      extract_json: true

tasks:
  - "tasks/*.yaml"
