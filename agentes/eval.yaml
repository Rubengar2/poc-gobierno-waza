name: security-agent-eval
skill: security-reviewer
schemaVersion: "1.2"
version: "1.0"

config:
  executor: mock
  timeout_seconds: 60
  trials_per_task: 1

graders:
  - name: formato_respuesta
    type: json_schema
    config:
      schema_file: output_schema.json
      extract_json: true

tasks:
  - "tasks/*.yaml"
